// Minimmum
let minWaarde = function(a, b) {      //parameters a b
  return Math.min(a, b);              // geef de laagste cijfer terug

}

console.log(minWaarde(2, 90));        // geeft argumenten door naar de functie
